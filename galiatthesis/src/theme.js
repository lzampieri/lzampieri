import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    "palette": {
        "background": {
            "paper": "#d8bcc0",
            "default": "#fafafa"
        },
        "primary": {
            "main": "#a8191b",
            "contrastText": "#fff"
        },
        "secondary": {
            "main": "#f4bd9e",
            "contrastText": "#6d2126"
        }
    }  
});

export default theme;
